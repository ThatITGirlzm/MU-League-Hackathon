[project]
name = "backend"
version = "0.1.0"
description = "MU League Hackathon Backend API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "httpx>=0.28.1",
    "pydantic-settings>=2.0.0",
]
